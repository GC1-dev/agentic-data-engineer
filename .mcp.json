{
  "mcpServers": {
    "databricks-utils": {
      "command": "poetry",
      "args": [
        "-C",
        "databricks-utils",
        "run",
        "python",
        "-m",
        "databricks_utils.mcp.server"
      ],
      "env": {
        "DATABRICKS_HOST": "${DATABRICKS_HOST}",
        "DATABRICKS_CONFIG_PROFILE": "${DATABRICKS_CONFIG_PROFILE}",
        "DATABRICKS_WAREHOUSE_ID": "${DATABRICKS_WAREHOUSE_ID}"
      }
    }
  }
}